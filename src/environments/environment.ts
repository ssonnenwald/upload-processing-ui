export const environment = {
  production: true,
  // Empty base means relative URLs — assumes the API is hosted on the same origin
  // as the UI (typical production deployment behind a single reverse proxy).
  apiBaseUrl: '',
  hubUrl: '/hubs/run-status',
};
